// Calculate the simple interestss

#include<stdio.h>
int main(){
    int principal = 100, rate = 4,years=1;
    int si = (principal*rate*years)/100;
    printf("the value of simple interest is %d",si);
   
    return 0;
}